const DEFAULT_PROMPT= "Describe the image using concise, comma-separated semantic keywords only, no sentences, no filler, focus on objects, actions, setting, style, and notable attributes, optimize for efficient semantic search.";
const DEFAULT_MODEL= "gpt-5-nano";
const DEFAULT_SCALE= 720;

export { DEFAULT_PROMPT, DEFAULT_MODEL, DEFAULT_SCALE };